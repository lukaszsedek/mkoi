package pl.edu.pw.tele.frontend;

import javax.print.attribute.standard.MediaSize.Engineering;
import javax.swing.JList;
import javax.swing.JPanel;

import pl.edu.pw.tele.enigma.Enigma;
import pl.edu.pw.tele.enigma.Reflector;

public class MKOICenter extends JPanel {
	
	protected JList nameList;
	protected String[] dupa = new String[]{"empty"};

	protected String[] rotorAlhpabet;
	
	
	public MKOICenter()
	{
		
		Enigma enigma = Enigma.getInstance();
		Reflector reflector = enigma.getReflector();
		rotorAlhpabet = new String[reflector.getAlfabet().length];
		
		for (int i = 0; i < reflector.getAlfabet().length; ++i)
		{	
			rotorAlhpabet[i] = String.copyValueOf(enigma.getAlphabet());
		}
		
		nameList = new JList(rotorAlhpabet);
		add(nameList);
	}
			
}
