package pl.edu.pw.tele.frontend;

import java.awt.LayoutManager;

import javax.swing.BoxLayout;
import javax.swing.JLabel;
import javax.swing.JPanel;
import javax.swing.JScrollPane;
import javax.swing.JTextArea;

/**
 * 
 * @author lukaszsedek
 * GUI component for showing input text
 */
public class InputTextPane extends JScrollPane implements IInputTextPane{

	protected JLabel title = new JLabel("dupa");
	protected JTextArea inputArea = new JTextArea(30, 30);
	
	public InputTextPane()
	{
		setLayout((LayoutManager) new BoxLayout(this, BoxLayout.PAGE_AXIS));
		add(title);
		add(inputArea);
	}
	
}
